// This file was auto-generated by Fern from our API Definition.

/**
 * Code package status for the Function.
 */
export interface FunctionCodePackageResponseBody {
    /** A presigned GET URL for downloading the function's current code package. Present only when code has been uploaded. */
    downloadGetUrl?: string | undefined;
    /** Status of the code package.  Valid values: `unknown`, `pendingUpload`, `uploaded`, `deployed` */
    status: FunctionCodePackageResponseBody.Status;
}

export namespace FunctionCodePackageResponseBody {
    /** Status of the code package.  Valid values: `unknown`, `pendingUpload`, `uploaded`, `deployed` */
    export const Status = {
        Unknown: "unknown",
        PendingUpload: "pendingUpload",
        Uploaded: "uploaded",
        Deployed: "deployed",
    } as const;
    export type Status = (typeof Status)[keyof typeof Status];
}
