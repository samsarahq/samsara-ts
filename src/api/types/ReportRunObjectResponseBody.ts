// This file was auto-generated by Fern from our API Definition.

/**
 * Base custom report run response object.
 */
export interface ReportRunObjectResponseBody {
    /** Time when the report run was created in RFC3339 format. */
    createdAtTime: string;
    /** ID of report run. */
    id: string;
    /** Status of the report run.  Valid values: `complete`, `pending`, `failed`, `canceled`, `unknown` */
    status: ReportRunObjectResponseBody.Status;
    /** Time when the report run was last updated in RFC3339 format. */
    updatedAtTime: string;
}

export namespace ReportRunObjectResponseBody {
    /** Status of the report run.  Valid values: `complete`, `pending`, `failed`, `canceled`, `unknown` */
    export const Status = {
        Complete: "complete",
        Pending: "pending",
        Failed: "failed",
        Canceled: "canceled",
        Unknown: "unknown",
    } as const;
    export type Status = (typeof Status)[keyof typeof Status];
}
