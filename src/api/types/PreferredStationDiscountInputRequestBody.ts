// This file was auto-generated by Fern from our API Definition.

import type * as Samsara from "../index.js";

/**
 * A negotiated discount for a specific fuel type at this station.
 */
export interface PreferredStationDiscountInputRequestBody {
    discount?: Samsara.PreferredStationDiscountMoneyRequestBody | undefined;
    /** The discount percentage. Used for percentage discount type. */
    discountPercent?: string | undefined;
    /** How the discount is calculated.  Valid values: `centsPerUnit`, `percentage`, `fixedPrice` */
    discountType: PreferredStationDiscountInputRequestBody.DiscountType;
    /** The fuel type this discount applies to.  Valid values: `gasoline`, `diesel`, `biodiesel`, `flexibleFuelE85`, `ethanolE100`, `renewableDiesel`, `m100`, `cng`, `lng`, `lpg`, `rng`, `fcev`, `unknown` */
    fuelType: PreferredStationDiscountInputRequestBody.FuelType;
    /** Volume/energy unit. Required for centsPerUnit and fixedPrice.  Valid values: `liter`, `gallon`, `imperialGallon` */
    volumeUnit?: PreferredStationDiscountInputRequestBody.VolumeUnit | undefined;
}

export namespace PreferredStationDiscountInputRequestBody {
    /** How the discount is calculated.  Valid values: `centsPerUnit`, `percentage`, `fixedPrice` */
    export const DiscountType = {
        CentsPerUnit: "centsPerUnit",
        Percentage: "percentage",
        FixedPrice: "fixedPrice",
    } as const;
    export type DiscountType = (typeof DiscountType)[keyof typeof DiscountType];
    /** The fuel type this discount applies to.  Valid values: `gasoline`, `diesel`, `biodiesel`, `flexibleFuelE85`, `ethanolE100`, `renewableDiesel`, `m100`, `cng`, `lng`, `lpg`, `rng`, `fcev`, `unknown` */
    export const FuelType = {
        Gasoline: "gasoline",
        Diesel: "diesel",
        Biodiesel: "biodiesel",
        FlexibleFuelE85: "flexibleFuelE85",
        EthanolE100: "ethanolE100",
        RenewableDiesel: "renewableDiesel",
        M100: "m100",
        Cng: "cng",
        Lng: "lng",
        Lpg: "lpg",
        Rng: "rng",
        Fcev: "fcev",
        Unknown: "unknown",
    } as const;
    export type FuelType = (typeof FuelType)[keyof typeof FuelType];
    /** Volume/energy unit. Required for centsPerUnit and fixedPrice.  Valid values: `liter`, `gallon`, `imperialGallon` */
    export const VolumeUnit = {
        Liter: "liter",
        Gallon: "gallon",
        ImperialGallon: "imperialGallon",
    } as const;
    export type VolumeUnit = (typeof VolumeUnit)[keyof typeof VolumeUnit];
}
