// This file was auto-generated by Fern from our API Definition.

import type * as Samsara from "../../../../index.js";

/**
 * @example
 *     {
 *         id: "id"
 *     }
 */
export interface EntityWarrantyClaimsServiceUpdateWarrantyClaimRequestBody {
    /** Unique identifier for the WarrantyClaim record. */
    id: string;
    /** ID of the asset the claim is filed for. Immutable once set. */
    assetId?: string;
    /** The cause of the 3 Cs - the root cause found. */
    cause?: string;
    /** Engine hours at the time of repair. */
    claimEngineHours?: number;
    /** Asset odometer reading at the time of repair. Measured in meters. */
    claimOdometerMeters?: number;
    /** Current status of the claim. */
    claimStatus?: string;
    /** IDs of asset component instances covered by this claim. */
    componentInstanceIds?: string[];
    /** The concern of the 3 Cs - what was reported. */
    concern?: string;
    /** The correction of the 3 Cs - the work performed. */
    correction?: string;
    /** Customer-supplied external identifiers for the warranty claim, interchangeable with id in filters. Only included in the response when includeExternalIds is set. */
    externalIds?: Samsara.UpdateWarrantyClaimEntityWarrantyClaimWarrantyClaimExternalIdInputTypeRequestBody[];
    /** Labor being claimed. */
    labor?: Samsara.UpdateWarrantyClaimEntityWarrantyClaimWarrantyClaimLaborInputTypeRequestBody[];
    /** ID of the warranty this claim is filed against. */
    linkedWarrantyId?: string;
    /** IDs of the work orders associated with this claim. */
    linkedWorkOrderIds?: string[];
    /** IDs of media items attached to the claim. */
    mediaItemIds?: string[];
    otherCost?: Samsara.UpdateWarrantyClaimEntityWarrantyClaimMoneyInputTypeRequestBody;
    /** Parts being claimed. */
    parts?: Samsara.UpdateWarrantyClaimEntityWarrantyClaimWarrantyClaimPartInputTypeRequestBody[];
    /** When reimbursement was received. */
    reimbursedAtTime?: string;
    /** Reimbursement amounts, optionally linked to a work order. */
    reimbursements?: Samsara.UpdateWarrantyClaimEntityWarrantyClaimClaimReimbursementInputTypeRequestBody[];
    /** When the repair was completed. */
    repairCompletedAtTime?: string;
    /** When the claim was resolved. */
    resolutionAtTime?: string;
    /** When the claim was submitted to the vendor. */
    submittedAtTime?: string;
    /** ID of the vendor handling the claim. */
    warrantyVendorId?: string;
}
