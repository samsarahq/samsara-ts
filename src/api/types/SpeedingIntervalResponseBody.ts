/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as SamsaraApi from "../index.js";

/**
 * Speeding Interval Object
 */
export interface SpeedingIntervalResponseBody {
    /** UTC time the interval ended in RFC 3339 format. */
    endTime: string;
    /** Whether the interval is dismissed. */
    isDismissed: boolean;
    location: SamsaraApi.SpeedingIntervalLocationResponseResponseBody;
    /** The max speed exceeded for the speeding interval. */
    maxSpeedKilometersPerHour: number;
    /** The posted speed limit associated with the speeding interval. */
    postedSpeedLimitKilometersPerHour: number;
    /** Specifies the severity level of the speeding interval.  Valid values: `light`, `moderate`, `heavy`, `severe` */
    severityLevel: SpeedingIntervalResponseBody.SeverityLevel;
    /** UTC time the interval started in RFC 3339 format. */
    startTime: string;
}

export namespace SpeedingIntervalResponseBody {
    /**
     * Specifies the severity level of the speeding interval.  Valid values: `light`, `moderate`, `heavy`, `severe`
     */
    export type SeverityLevel = "light" | "moderate" | "heavy" | "severe";
    export const SeverityLevel = {
        Light: "light",
        Moderate: "moderate",
        Heavy: "heavy",
        Severe: "severe",
    } as const;
}
