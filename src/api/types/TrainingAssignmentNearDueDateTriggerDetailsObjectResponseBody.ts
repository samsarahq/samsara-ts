/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as SamsaraApi from "../index.js";

/**
 * Details specific to Training Assignment Near Due Date
 */
export interface TrainingAssignmentNearDueDateTriggerDetailsObjectResponseBody {
    /** The assignment groups the trigger is configured for. */
    assignmentGroups?: SamsaraApi.TrainingAssignmentNearDueDateTriggerAssignmentGroupObjectResponseBody[];
    /** Whether the trigger is configured in days or weeks.  Valid values: `DAYS`, `WEEKS` */
    conditionUnits: TrainingAssignmentNearDueDateTriggerDetailsObjectResponseBody.ConditionUnits;
    /** The number of days or weeks near the due date to trigger on. */
    conditionValue: number;
    /** The timezone that the alert will be set up in. */
    timezone: string;
}

export namespace TrainingAssignmentNearDueDateTriggerDetailsObjectResponseBody {
    /**
     * Whether the trigger is configured in days or weeks.  Valid values: `DAYS`, `WEEKS`
     */
    export type ConditionUnits = "DAYS" | "WEEKS";
    export const ConditionUnits = {
        Days: "DAYS",
        Weeks: "WEEKS",
    } as const;
}
