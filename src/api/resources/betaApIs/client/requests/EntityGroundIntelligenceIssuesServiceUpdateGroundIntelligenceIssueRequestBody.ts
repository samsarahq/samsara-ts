// This file was auto-generated by Fern from our API Definition.

/**
 * @example
 *     {
 *         id: "id"
 *     }
 */
export interface EntityGroundIntelligenceIssuesServiceUpdateGroundIntelligenceIssueRequestBody {
    /** Unique identifier for the Ground Intelligence issue. */
    id: string;
    /** Optional note about the dismissal. Set to null to clear. */
    dismissalNote?: string | null;
    /** Reason for dismissing the issue. Required when status is dismissed.  Valid values: `notMyJurisdiction`, `knownIssue`, `duplicate`, `inaccurateDetection`, `other` */
    dismissalReason?: EntityGroundIntelligenceIssuesServiceUpdateGroundIntelligenceIssueRequestBody.DismissalReason;
    /** Customer-facing review status for the issue.  Valid values: `needsReview`, `reviewed`, `resolved`, `dismissed` */
    status?: EntityGroundIntelligenceIssuesServiceUpdateGroundIntelligenceIssueRequestBody.Status;
    /** Customer-facing road-condition type for the issue.  Valid values: `pothole`, `roadCracking`, `patchedPothole` */
    type?: EntityGroundIntelligenceIssuesServiceUpdateGroundIntelligenceIssueRequestBody.Type;
}

export namespace EntityGroundIntelligenceIssuesServiceUpdateGroundIntelligenceIssueRequestBody {
    /** Reason for dismissing the issue. Required when status is dismissed.  Valid values: `notMyJurisdiction`, `knownIssue`, `duplicate`, `inaccurateDetection`, `other` */
    export const DismissalReason = {
        NotMyJurisdiction: "notMyJurisdiction",
        KnownIssue: "knownIssue",
        Duplicate: "duplicate",
        InaccurateDetection: "inaccurateDetection",
        Other: "other",
    } as const;
    export type DismissalReason = (typeof DismissalReason)[keyof typeof DismissalReason];
    /** Customer-facing review status for the issue.  Valid values: `needsReview`, `reviewed`, `resolved`, `dismissed` */
    export const Status = {
        NeedsReview: "needsReview",
        Reviewed: "reviewed",
        Resolved: "resolved",
        Dismissed: "dismissed",
    } as const;
    export type Status = (typeof Status)[keyof typeof Status];
    /** Customer-facing road-condition type for the issue.  Valid values: `pothole`, `roadCracking`, `patchedPothole` */
    export const Type = {
        Pothole: "pothole",
        RoadCracking: "roadCracking",
        PatchedPothole: "patchedPothole",
    } as const;
    export type Type = (typeof Type)[keyof typeof Type];
}
