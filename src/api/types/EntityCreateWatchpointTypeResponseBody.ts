// This file was auto-generated by Fern from our API Definition.

import type * as Samsara from "../index.js";

/**
 * Watchpoint object
 */
export interface EntityCreateWatchpointTypeResponseBody {
    /** Time when the watchpoint resource was created. */
    createdAtTime: string;
    /** Unique identifier for the watchpoint. */
    id: string;
    /** Time when the most recent observation was collected, or null when unavailable. */
    lastObservationTime: string | null;
    location: Samsara.WatchpointLatLngTypeResponseBody;
    /** Recurrence frequency for observations.  Valid values: `unknown`, `justOnce`, `daily`, `weekly`, `monthly` */
    mode: EntityCreateWatchpointTypeResponseBody.Mode;
    /** Server-derived end of the current monitoring window. */
    monitoringEndTime: string;
    /** Start of the current monitoring run. */
    monitoringStartTime: string;
    /** Customer-provided name for the watchpoint, or null when unavailable. */
    name: string | null;
    /** Customer-provided note about the watchpoint, or null when unavailable. */
    note: string | null;
    /** Number of observations collected for the watchpoint. */
    observationCount: number;
    /** Type of condition to observe at this watchpoint.  Valid values: `unknown`, `roadDefect`, `utilityCut`, `guardrail`, `streetlight`, `signage`, `stormDrain`, `graffiti`, `vegetation`, `blight`, `illegalDumping`, `littering`, `highVegetationWeeds`, `fire`, `other` */
    observationType: EntityCreateWatchpointTypeResponseBody.ObservationType;
    /** Organization-scoped URL that opens this Watchpoint in the Samsara dashboard. */
    samsaraDashboardUrl: string;
    /** Current lifecycle status of the watchpoint.  Valid values: `unknown`, `active`, `completed` */
    status: EntityCreateWatchpointTypeResponseBody.Status;
    /** Time when the public watchpoint projection was most recently updated. */
    updatedAtTime: string;
}

export namespace EntityCreateWatchpointTypeResponseBody {
    /** Recurrence frequency for observations.  Valid values: `unknown`, `justOnce`, `daily`, `weekly`, `monthly` */
    export const Mode = {
        Unknown: "unknown",
        JustOnce: "justOnce",
        Daily: "daily",
        Weekly: "weekly",
        Monthly: "monthly",
    } as const;
    export type Mode = (typeof Mode)[keyof typeof Mode];
    /** Type of condition to observe at this watchpoint.  Valid values: `unknown`, `roadDefect`, `utilityCut`, `guardrail`, `streetlight`, `signage`, `stormDrain`, `graffiti`, `vegetation`, `blight`, `illegalDumping`, `littering`, `highVegetationWeeds`, `fire`, `other` */
    export const ObservationType = {
        Unknown: "unknown",
        RoadDefect: "roadDefect",
        UtilityCut: "utilityCut",
        Guardrail: "guardrail",
        Streetlight: "streetlight",
        Signage: "signage",
        StormDrain: "stormDrain",
        Graffiti: "graffiti",
        Vegetation: "vegetation",
        Blight: "blight",
        IllegalDumping: "illegalDumping",
        Littering: "littering",
        HighVegetationWeeds: "highVegetationWeeds",
        Fire: "fire",
        Other: "other",
    } as const;
    export type ObservationType = (typeof ObservationType)[keyof typeof ObservationType];
    /** Current lifecycle status of the watchpoint.  Valid values: `unknown`, `active`, `completed` */
    export const Status = {
        Unknown: "unknown",
        Active: "active",
        Completed: "completed",
    } as const;
    export type Status = (typeof Status)[keyof typeof Status];
}
