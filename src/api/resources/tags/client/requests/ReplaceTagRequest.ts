/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as SamsaraApi from "../../../../index.js";

/**
 * @example
 *     {}
 */
export interface ReplaceTagRequest {
    /** The addresses that belong to this tag. */
    addresses?: SamsaraApi.TaggedObjectId[];
    /** The trailers, unpowered, and powered assets that belong to this tag. */
    assets?: SamsaraApi.TaggedObjectId[];
    /** The drivers that belong to this tag. */
    drivers?: SamsaraApi.TaggedObjectId[];
    /** The machines that belong to this tag. */
    machines?: SamsaraApi.TaggedObjectId[];
    /** Name of this tag. */
    name?: string;
    /** If this tag is part a hierarchical tag tree, this is the ID of the parent tag, otherwise this will be omitted. */
    parentTagId?: string;
    /** The sensors that belong to this tag. */
    sensors?: SamsaraApi.TaggedObjectId[];
    /** The vehicles that belong to this tag. */
    vehicles?: SamsaraApi.TaggedObjectId[];
}
