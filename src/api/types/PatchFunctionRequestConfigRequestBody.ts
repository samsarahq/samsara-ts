// This file was auto-generated by Fern from our API Definition.

import type * as Samsara from "../index.js";

/**
 * Configuration fields to update on the Function. All fields are optional; only provided fields are updated.
 */
export interface PatchFunctionRequestConfigRequestBody {
    /** The handler entrypoint for the function code (e.g. 'index.handler'). */
    handler?: string | undefined;
    /** Whether the run schedule is enabled. */
    isScheduleEnabled?: boolean | undefined;
    /** Default parameter values for the function. Replaces all existing parameters. */
    params?: PatchFunctionRequestConfigRequestBody.Params | undefined;
    schedule?: Samsara.FunctionScheduleRequestBody | undefined;
    /** Secrets available to the function at runtime. Replaces all existing secrets. */
    secrets?: PatchFunctionRequestConfigRequestBody.Secrets | undefined;
}

export namespace PatchFunctionRequestConfigRequestBody {
    /**
     * Default parameter values for the function. Replaces all existing parameters.
     */
    export type Params = {};

    /**
     * Secrets available to the function at runtime. Replaces all existing secrets.
     */
    export type Secrets = {};
}
