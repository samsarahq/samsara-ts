// This file was auto-generated by Fern from our API Definition.

import type * as Samsara from "../index.js";

/**
 * Training Course response object.
 */
export interface TrainingCourseResponseObjectResponseBody {
    category: Samsara.TrainingCategoryObjectResponseBody;
    /** Description of the course. */
    description?: string;
    /** Estimated time it takes to complete the course. */
    estimatedTimeToCompleteMinutes: number;
    /** ID of the training course. */
    id: string;
    /** List of course labels. */
    labels?: Samsara.TrainingCourseLabelObjectResponseBody[];
    /** ID of the course's specific version. */
    revisionId: string;
    /**
     * Status of the training course. Always returned.
     * Note: Only courses in a ‘published’ state are assignable to learners. Archiving a course deletes only ‘inProgress’ assignments associated with that course, and deleting a course deletes all assignments associated with that course.  Valid values: `published`, `deleted`, `archived`, `unknown`
     */
    status: TrainingCourseResponseObjectResponseBody.Status;
    /** Title of the course. */
    title: string;
}

export namespace TrainingCourseResponseObjectResponseBody {
    /**
     * Status of the training course. Always returned.
     * Note: Only courses in a ‘published’ state are assignable to learners. Archiving a course deletes only ‘inProgress’ assignments associated with that course, and deleting a course deletes all assignments associated with that course.  Valid values: `published`, `deleted`, `archived`, `unknown`
     */
    export const Status = {
        Published: "published",
        Deleted: "deleted",
        Archived: "archived",
        Unknown: "unknown",
    } as const;
    export type Status = (typeof Status)[keyof typeof Status];
}
